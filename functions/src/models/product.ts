export interface Product {
  uId?: string;
  name: string;
  inStock :number;
  estimatedShipping: number;
  description: string;
  cost: number;
  img: string;
}//name,inStock,img,estimatedShipping,description,cost
